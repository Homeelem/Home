rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to authenticated users, write access to all
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if true;
    }
  }
}
